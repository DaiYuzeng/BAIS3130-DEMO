Flask==2.3.2                 # Web framework for handling HTTP requests
flask-blueprint==1.1.2       # Optional, for better modularization with Blueprints
flask-cors==3.0.10           # Cross-Origin Resource Sharing, if you need your API accessible from other domains
flask-restful==0.3.9         # Optional, for easy REST API creation
SQLAlchemy==2.0.16           # ORM for managing database connections (if you want to use ORM instead of raw SQL)
marshmallow==3.19.0          # For data serialization and validation
requests==2.31.0             # For making HTTP requests (if your backend needs to communicate with other APIs)

# Testing and Development
pytest==7.4.2                # Testing framework
coverage==7.3.1              # Code coverage tool for unit tests
black==23.9.1                # Code formatter

